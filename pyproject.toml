[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "wordle"
version = "0.1.0"
description = "A Wordle clone with a programmatic interface for testing solvers."
requires-python = ">=3.9"

[tool.setuptools]
packages = ["wordle"]
